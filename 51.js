// default parameters

const addTwoNumbers = (num1=5, num2=9)=>{
    console.log(`Sum is ${num1+num2}`)
}

// addTwoNumbers(1)

const multiplyTwoNumbers = (a=10,b=10)=>console.log(`Multiple of two number is ${a*b}`)
multiplyTwoNumbers(1)